<%= form_with(model: movement_mode) do |form| %>
  <% if movement_mode.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(movement_mode.errors.count, "error") %> prohibited this movement_mode from being saved:</h2>

      <ul>
        <% movement_mode.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
