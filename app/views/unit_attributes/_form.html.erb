<%= form_with(model: unit_attribute) do |form| %>
  <% if unit_attribute.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(unit_attribute.errors.count, "error") %> prohibited this unit_attribute from being saved:</h2>

      <ul>
        <% unit_attribute.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :unit, style: "display: block" %>
    <%= form.number_field :unit %>
  </div>

  <div>
    <%= form.label :attribute_type, style: "display: block" %>
    <%= form.number_field :attribute_type %>
  </div>

  <div>
    <%= form.label :value, style: "display: block" %>
    <%= form.number_field :value %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
