<%= form_with(model: unit_role) do |form| %>
  <% if unit_role.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(unit_role.errors.count, "error") %> prohibited this unit_role from being saved:</h2>

      <ul>
        <% unit_role.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :unit, style: "display: block" %>
    <%= form.number_field :unit %>
  </div>

  <div>
    <%= form.label :role, style: "display: block" %>
    <%= form.number_field :role %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
